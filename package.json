{
  "name": "lexinx-protect",
  "version": "1.0.0",
  "description": "LEXINX two-layer script delivery server",
  "main": "server.js",
  "scripts": {
    "build": "npm run build server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
